import pyautogui
import cmd
import llama_index
import secrets

class Long_term_memory:
    def __init__(self, input):
       self.input = input
    def save_to_file(self):
       file = open(f"index{secrets.token_hex(2).json)

class AGI_AGENT_SHELL:
    pass